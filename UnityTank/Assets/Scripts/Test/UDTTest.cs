using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Vuforia;

public class UDTTest : MonoBehaviour
{
	UserDefinedTargetBuildingBehaviour UDTFactory;
	// Use this for initialization
	void Start ()
	{
//		UDTFactory = GetComponent<UserDefinedTargetBuildingBehaviour> ();
//		IUserDefinedTargetEventHandler[] target = FindObjectsOfType (typeof(IUserDefinedTargetEventHandler)) as IUserDefinedTargetEventHandler[];
//		for (int i = 0; i < target.Length; i++) {
//			UDTFactory.RegisterEventHandler (target [i]);
//		}
	}
	
	// Update is called once per frame
	void Update ()
	{
		
	}
}
